<Window x:Class="SistemaDeVenta.VentanaEditarProducto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar Producto"
        Height="550" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#121624" FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PanelDark" Color="#1C2132"/>
        <SolidColorBrush x:Key="AccentRed" Color="#FF334D"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8B92A5"/>
        <SolidColorBrush x:Key="BorderDark" Color="#2D3348"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0D1117"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDark}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,5,0,15"/>
        </Style>
    </Window.Resources>

    <Border Padding="30" Background="{StaticResource PanelDark}" CornerRadius="15" Margin="20">
        <StackPanel>
            <TextBlock Text="EDITAR PRODUCTO" Foreground="{StaticResource AccentRed}" 
                       FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Nombre del Producto" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
            <TextBox x:Name="txtNombre"/>

            <TextBlock Text="Categoría" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
            <ComboBox x:Name="cbCategoria" Margin="0,5,0,15" Padding="5">
                <ComboBoxItem Content="FRUTAS"/>
                <ComboBoxItem Content="VERDURAS"/>
                <ComboBoxItem Content="OTROS"/>
            </ComboBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Stock Actual" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                    <TextBox x:Name="txtStock"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Precio Compra" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                    <TextBox x:Name="txtCompra"/>
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Text="Precio Venta" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                    <TextBox x:Name="txtVenta"/>
                </StackPanel>
            </Grid>

            <Button Content="GUARDAR CAMBIOS" 
                    Background="{StaticResource AccentRed}" 
                    Foreground="White" 
                    FontWeight="Bold"
                    Padding="10" 
                    Margin="0,10,0,0"
                    Cursor="Hand"
                    Click="BtnGuardar_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Border>
</Window>